module.exports = {
	name: 'voiceStateUpdate',
	async execute(oldState, newState) {
        console.log(oldState);
        console.log(newState);
        console.log(124156);
        console.log(oldState.id);
        console.log(newState.id);
        // if (nanako.user.tag == nanako.user.tag) return;
		// try{
        //     console.log(`Someone joined voice at ${user.tag}`);
        // } catch (error) {
        //     console.error(error)
        // }
	}
};
